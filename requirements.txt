# Core PyTorch and ML dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Scientific computing and data processing
numpy>=1.21.0
scipy>=1.7.0
einops>=0.6.0

# Computer vision and image processing
opencv-python>=4.5.0
Pillow>=8.0.0

# Model loading and serialization
safetensors>=0.3.0
transformers>=4.20.0
accelerate>=0.20.0
xformers>=0.0.20

# Text processing and tokenization
tokenizers>=0.13.3
sentencepiece>=0.1.99

# Network and async support
aiohttp>=3.11.8
yarl>=1.18.0

# Configuration and utilities
pyyaml>=6.0
tqdm>=4.64.0
psutil>=5.9.0

# Advanced ML features
torchsde>=0.2.6

# Memory management and optimization (for VRAM features)
# Note: These are typically included with PyTorch but explicitly listed for clarity
# torch.cuda.memory_management functions
# torch.cuda.empty_cache
# torch.cuda.memory_allocated
# torch.cuda.memory_reserved

# Logging and monitoring (built-in Python modules)
# logging
# gc (garbage collection)
# weakref
# typing (type hints)

# Optional: Performance monitoring tools
# nvidia-ml-py3  # For detailed GPU monitoring (optional)
# GPUtil         # For GPU utilization monitoring (optional)
